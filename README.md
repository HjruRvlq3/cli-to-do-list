# CLI To-Do List

A console application written in Go for managing a to-do list.  
Allows you to add, view, and delete tasks directly from the command line.

---

## Features

- Add a new task
- View all tasks
- Delete a task by its number
- Runs in a loop until the `exit` command

---

## Installation and Running

Clone the repository:

```bash
git clone
